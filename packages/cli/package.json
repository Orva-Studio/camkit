{
  "name": "@camkit/cli",
  "version": "0.1.1",
  "type": "module",
  "bin": {
    "camkit": "./src/camkit.ts"
  },
  "scripts": {
    "build": "bun build --compile --outfile dist/camkit ./src/camkit.ts"
  },
  "dependencies": {
    "@camkit/core": "workspace:*",
    "@camkit/darwin": "workspace:*"
  }
}
